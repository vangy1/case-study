spring:
  datasource:
    url: jdbc:h2:mem:rohlik_test;mode=PostgreSQL;database_to_lower=true;
    driverClassName: org.h2.Driver
    username: admin
    password: password
logging:
  level:
    cz.rohlik: INFO

